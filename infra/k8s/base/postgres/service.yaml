apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  # Headless, the conventional pairing for a StatefulSet's serviceName.
  clusterIP: None
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
